# Load project-specific env vars
dotenv

# Activate GCP configuration for this repo (session-scoped)
export CLOUDSDK_ACTIVE_CONFIG_NAME="insilicostrategy"
export CLOUDSDK_CORE_PROJECT="insilicostrategy-platform"
